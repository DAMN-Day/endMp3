import 'package:flutter/material.dart';

class AppColors {
  // --- Colores de Marca ---
  static const Color primary = Color.fromARGB(255, 78, 8, 255); // Tu color de elección (ej. Morado Neón)
  static const Color accent = Color(0xFF03DAC6);

  // --- Fondos y Superficies ---
  static const Color background = Color(0xFF121212); // Fondo principal
  static const Color surface = Color(0xFF1E1E1E);    // Tarjetas y MiniPlayer
  static const Color cardGrey = Color(0xFF282828);   // Para GridView

  // --- Textos ---
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFB3B3B3);
  
  // --- Estados ---
  static const Color error = Color(0xFFCF6679);
}